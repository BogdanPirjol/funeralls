{
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "moment": "^2.30.1",
    "nodemon": "^3.1.10",
    "puppeteer": "^24.12.0"
  },
  "scripts": {
    "dev": "nodemon app.js",
    "build": "npm i && cd funeralls && npm i && npm run build && cd ..&& npx puppeteer browsers install chrome",
    "prod": "node app.js"
  }
}
