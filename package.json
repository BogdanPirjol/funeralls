{
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "moment": "^2.30.1",
    "nodemon": "^3.1.10",
    "puppeteer": "^24.12.0"
  },
  "scripts": {
    "postinstall": "npx puppeteer browsers install chrome",
    "dev": "nodemon app.js",
    "build": "npm i && cd funeralls && npm i && npm run build",
    "prod": "node app.js"
  }
}
